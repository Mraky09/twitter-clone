defmodule TwitterCloneApiWeb.SessionControllerTest do
  use TwitterCloneApiWeb.ConnCase

  describe "create/2" do
    test "Creates, and responses with a newly created token if username and password are correct"
    test "Returnns an error and does not create a token if username and password are incorrect"
  end
end
